import {styled} from "config/stitches.config";

export const ReadMore = styled("a", {
  color: "$primary",
  "&:visited": {
    color: "$primary",
  },
  "&:focus": {
    color: "$primary",
  },
  "&:active": {
    color: "$primary",
  },
});
