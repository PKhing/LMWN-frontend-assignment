import {styled} from "config/stitches.config";

export const Container = styled("div", {
  margin: "auto",
  marginTop: "50px",
  maxWidth: "700px",

  display: "flex",
  flexDirection: "column",
  gap: "20px",
});
